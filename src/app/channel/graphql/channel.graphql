directive @upper on FIELD_DEFINITION

type Channel {
    id: Int!
    name: String @upper
    creator: User
    creationDate: Date
}

type Query {
    channel(id: Int!): Channel
}

type Mutation {
    postChannel(name: String!, creatorId: Int!): Channel
}

