scalar Date
directive @upper on FIELD_DEFINITION

type Message {
    id: Int!
    content: String
    creationDate: Date
    sender: User
    channel: Channel
}

type Query {
    message(id: Int!): Message
}

type Mutation {
    postMessage(content: String!, senderId: Int!, channelId: Int!): Message
}

type Subscription {
    messageAdded(channel: String!): Message
}
