mutation PostNewChannel($name: String!, $creatorId: Int!) {
    postChannel(name: $name, creatorId:  $creatorId) {
        id,
        name,
        creationDate,
        creator {
            id,
            name
        }
    }
}
